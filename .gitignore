node_modules/
dist/
.angular/
*.log
.env
.env.local
login.md
.DS_Store

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/mcp.json.example
.idea/

# Docker volumes bind-mounted for local dev only
postgres-data/

# Local secret files for docker-compose.secrets.yml — never commit real secrets
/secrets/

# Playwright (e2e/)
/e2e/test-results/
/e2e/playwright-report/
/e2e/blob-report/
/e2e/.last-run.json

# Python IDE (ide/) - virtualenvs, bytecode and test caches
ide/.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Delegation transcripts - a machine-local record of what the local model was asked to do
# and what it did. Kept out of git: they are per-machine history, not project source.
logs/
