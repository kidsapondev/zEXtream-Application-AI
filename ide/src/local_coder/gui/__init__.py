"""The desktop window.

A PySide6 UI over the same `CoderBackend` the terminal app uses — only the presentation
layer differs, so workspace access, the review gate, git, the runner and the language server
are shared rather than reimplemented.
"""
